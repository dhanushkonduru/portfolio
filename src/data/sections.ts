/**
 * Sections are derived from the stage definitions, so the nav, the scroll-spy
 * and the 3D field can never disagree about what the page contains.
 */
export { NAV_STAGES as sections, STAGE_IDS } from "@/system/stages";
